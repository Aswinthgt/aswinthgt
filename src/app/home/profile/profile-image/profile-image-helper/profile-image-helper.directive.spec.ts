import { ProfileImageHelperDirective } from './profile-image-helper.directive';

describe('ProfileImageHelperDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfileImageHelperDirective();
    expect(directive).toBeTruthy();
  });
});
